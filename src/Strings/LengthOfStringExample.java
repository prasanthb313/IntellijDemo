package Strings;

public class LengthOfStringExample {
    public static void main(String[] args) {
        String str = "welcome";

        //int length = str.length();

        for (int i=0;i<=str.length()-1;i++)
        {
            System.out.println(str.charAt(i));
        }
    }
}
